{
  "name": "monospace-design-system",
  "version": "0.1.0",
  "description": "A monospace / terminal design style (Atom One Dark–inspired). Tokens are the single source of truth.",
  "type": "module",
  "scripts": {
    "build": "node build/build.js",
    "catalog": "npm run build && node build/serve-catalog.js"
  },
  "keywords": ["design-system", "design-tokens", "monospace", "terminal", "style-dictionary"],
  "license": "MIT",
  "devDependencies": {
    "style-dictionary": "^4.3.0"
  }
}
